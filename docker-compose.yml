version: "3"

services:
  app:
    build: .
    image: sumeet2211/nodeapp:latest
    container_name: node_app
    ports:
      - "9000:3000"
    depends_on:
      - mongo
    restart: always

  mongo:
    image: mongo
    container_name: mongo_db
    ports:
      - "27017:27017"
    restart: always

  nginx:
    image: nginx
    container_name: nginx_server
    ports:
      - "8081:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    restart: always
